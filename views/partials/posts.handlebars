<section class="card">
  <a href="/post/{{id}}">
    <header>{{title}}
      By:
      {{user.username}}
      Date Created: FIX TIME
      {{posts.created_at}}</header>
    <p>{{content}}</p>
  </a>
</section>